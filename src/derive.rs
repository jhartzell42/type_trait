use proc_macro2::TokenStream;
use quote::{quote, ToTokens};
use syn::{self, Attribute, Data, DataEnum, DeriveInput, Fields, Generics, Ident};

fn expand_derive(ast: DeriveInput) -> TokenStream {
    panic!("");
}
